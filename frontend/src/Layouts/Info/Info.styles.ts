import styled from "styled-components"

export const InfoWrapper = styled.div`

display:flex;
width:100%;
flex-direction:column;
gap:2rem;
align-items:center;
img{
    width:300px;
}
.main_text{
    font-weight:600;
    font-size:1.5rem;
    letter-spacing:1px;
    color:var(--gray);
}

`